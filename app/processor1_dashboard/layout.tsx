import { Processor1Shell } from "@/components/processor1/Processor1Shell";

export default function Processor1Layout({ children }: { children: React.ReactNode }) {
  return <Processor1Shell>{children}</Processor1Shell>;
}
