import { Processor2Shell } from "@/components/processor2/Processor2Shell";

export default function Processor2Layout({ children }: { children: React.ReactNode }) {
  return <Processor2Shell>{children}</Processor2Shell>;
}
