# =============================================================================
# DATABASE
# =============================================================================
# Production (Neon): paste the connection string from Neon Dashboard → Connect.
# Use the pooled string on Vercel/serverless if Neon offers it; include ?sslmode=require if needed.
# Local dev: point DATABASE_URL at the same Neon branch, or run a local Postgres and paste its URL.
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"

# =============================================================================
# SITE & DOMAIN (production)
# =============================================================================
# Public URL of your site — NO trailing slash. Required for payment links, emails, referrals, webhooks.
# Examples: https://www.filerightsolutions.com  or  https://filerightsolutions.com
SITE_BASE_URL="http://localhost:3000"

SITE_NAME="FileRight Solutions"
# Shown in the header; SEO home title becomes "BRAND — this" unless BRAND equals this (then a tagline is used).
NEXT_PUBLIC_SITE_NAME="Your Tax Filing Assistance"
# Optional: legal/SEO brand first in titles (defaults to FileRight Solutions)
# NEXT_PUBLIC_BRAND_NAME="FileRight Solutions"

# Optional: Meta (Facebook) Pixel ID for ads / analytics (defaults to production pixel if unset).
# NEXT_PUBLIC_META_PIXEL_ID="323597132246232"

# Optional: extra hosts allowed for Server Actions (comma or space separated), e.g. apex + www:
# SERVER_ACTION_ALLOWED_ORIGINS="https://yourdomain.com https://www.yourdomain.com"
# Usually unnecessary if SITE_BASE_URL is set — next.config.js also adds VERCEL_URL on Vercel.

# =============================================================================
# PROFESSIONAL EMAIL (matches your domain / email hosting)
# =============================================================================
# SUPPORT_EMAIL: shown to customers, Reply-To on billing mail, and default From display name target.
SUPPORT_EMAIL="support@yourdomain.com"

# OUTLOOK / MICROSOFT GRAPH (recommended for email-heavy sending)
# Create an Entra app registration with application permissions to send mail:
#   - Mail.Send
# Then grant admin consent in your Microsoft tenant.
#
# GRAPH_SENDER_USER = mailbox that actually sends (e.g. support@yourdomain.com)
# The same mailbox should match SPF/DKIM/DMARC alignment for best deliverability.
GRAPH_TENANT_ID=""
GRAPH_CLIENT_ID=""
GRAPH_CLIENT_SECRET=""
GRAPH_SENDER_USER="support@yourdomain.com"
GRAPH_SAVE_TO_SENT_ITEMS="true"

# Optional full From header shown to recipients. If unset, app uses:
#   "<SITE_NAME> <GRAPH_SENDER_USER>"
SMTP_FROM="FileRight Solutions <support@yourdomain.com>"

# Optional: BCC billing-quote emails only (comma-separated OK). Use your personal inbox to confirm
# Graph accepted the message even when the client’s mailbox filters or delays delivery.
# SMTP_BCC="you@gmail.com"

# Email safety/operations controls
# Pause all outbound sends globally (except local dev log-only mode).
EMAIL_PAUSED_ALL="false"
# Max attempts for scheduled emails before permanent failure.
EMAIL_MAX_ATTEMPTS="6"
# How many scheduled emails to process per cron run.
EMAIL_SCHEDULED_BATCH_SIZE="25"

# =============================================================================
# SESSION & ADMIN
# =============================================================================
SESSION_SECRET="replace_with_a_long_random_secret"

ADMIN_EMAIL="admin@yourdomain.com"
ADMIN_PASSWORD="replace_with_strong_password"

# =============================================================================
# PAYMENTS (Dragonpay)
# =============================================================================
DRAGONPAY_MERCHANT_ID=""
DRAGONPAY_SECRET=""

# =============================================================================
# PAYMENTS (PayMongo)
# =============================================================================
# Secret key used by server routes to create checkout sessions.
PAYMONGO_SECRET_KEY=""
# Optional public key (only needed for client-side PayMongo JS flows).
PAYMONGO_PUBLIC_KEY=""
# Webhook signing secret from PayMongo Dashboard → Webhooks → your endpoint → show/copy secret.
# When set, /api/paymongo/webhook rejects POSTs with a bad Paymongo-Signature (recommended in production).
PAYMONGO_WEBHOOK_SECRET=""
# Comma-separated checkout payment_method_types. Include qrph for BSP QR Ph (GCash/Maya/BPI/etc. via one QR).
# Example: qrph,gcash,paymaya,card,grab_pay
PAYMONGO_PAYMENT_METHOD_TYPES="qrph,gcash,paymaya"

# =============================================================================
# QUOTED BILLING (/admin/billing)
# =============================================================================
REFERRAL_FEE_REDUCTION_PERCENT="10"
MIN_QUOTED_PAYMENT_PHP="1"

# =============================================================================
# EXTERNAL AGENT REFERRALS (/agent-dashboard)
# =============================================================================
# Ledger payout amount (PHP) and minimum hours after submission before auto “payout completed” (customer must be paid).
# AGENT_REFERRAL_PAYOUT_PHP="100"
# AGENT_REFERRAL_PAYOUT_DELAY_HOURS="72"

# =============================================================================
# OTHER
# =============================================================================
CRON_KEY=""

# Optional: email admin a PDF on each 1701A submit (default: off). Set both to enable.
# EVALUATION_SUBMIT_NOTIFY_ADMIN="true"
# EVALUATION_PDF_NOTIFY_EMAIL="you@example.com"
